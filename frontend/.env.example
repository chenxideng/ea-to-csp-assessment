# Azure AD App Registration — SPA public client (no secret needed)
# Register at https://portal.azure.com → Azure Active Directory → App registrations
# Set platform to "Single-page application" with redirect URI: http://localhost:5173
VITE_AZURE_CLIENT_ID=your-app-client-id
